spring:
  application:
    name: jmstest
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configserver
      fail-fast: true
eureka:
  client:
    service-url:
      defaultZone: ${eureka_service_uri}